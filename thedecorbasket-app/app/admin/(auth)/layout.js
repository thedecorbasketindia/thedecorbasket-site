export const metadata = { title: "Admin Sign In — The Decor Basket", robots: "noindex, nofollow" };

export default function AuthLayout({ children }) {
  return <div className="admin-shell">{children}</div>;
}
